Завдання:

1.Створити свій власний публічний репозиторій
2.Стягнути його собі локально
3.В гілці master додати файл з назвою “назва групи” 
4.Зробити в файлі два коміти(будь які зміни, це необхідно для вашої практики)
5.Додати зміни до віддаленого репозиторію
6.Створити гілку “name.surname.gitpractice”
7.На вашій гілці додати зміни(будь які, наприклад до файлу додати новий рядок "Hello world"), та додати їх до віддаленого репозиторію
8.ДЗ здавати як посилання на ваш репозиторій.


GIT INIT

1. Перейдіть до директорії вашого проекту за допомогою команди `cd`, якщо ви ще не знаходитесь в потрібному місці.

2. Виконайте команду 

 git init

Це створить каталог `.git` в поточній директорії, де Git буде зберігати всю інформацію про репозиторій, включаючи історію комітів, гілки та інші дані.

3. Після виконання `git init`, ваш проект стане Git-репозиторієм, і Git буде відстежувати зміни в файлах цієї директорії.

GIT STATUS

Команда `git status` використовується для перевірки статусу вашого локального Git-репозиторію. Вона надає інформацію про стан файлів у вашому робочому каталозі (робоча директорія), індексі (staging area) та поточному гілці (branch). 

Ось як вона працює:

1. Запустіть командний рядок або термінал у директорії, де розташований ваш Git-репозиторій.

2. Виконайте команду `git status`:

git status